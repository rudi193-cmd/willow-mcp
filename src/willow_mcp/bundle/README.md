# Bundle seeds

Shipped inside the wheel for `willow-mcp-init`. Source of truth for editing:

- `templates/` ← `docs/templates/`
- `skills/` ← `skills/` + product-only skills here
- `hooks/` ← `hooks/`

Sync before release: copy repo sources into this directory.
